import { InlineAlert } from './InlineAlert';

export default {
  title: 'Molecules/InlineAlert',
  component: InlineAlert,
};

export const Default = {
  args: { message: 'Lembre-se que o pagamento deverá ser efetuado em até 3h depois da compra.' },
};
